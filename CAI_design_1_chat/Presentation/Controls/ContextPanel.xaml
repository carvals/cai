<UserControl x:Class="CAI_design_1_chat.Presentation.Controls.ContextPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="using:CAI_design_1_chat.Presentation.Controls">

    <Grid Background="{ThemeResource LayerFillColorDefaultBrush}">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Spacing="16" Margin="16">
                <!-- Header with Refresh Button -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    
                    <!-- Context Title -->
                    <TextBlock Grid.Column="0"
                               Text="Context" 
                               FontSize="20" 
                               FontWeight="SemiBold"
                               Foreground="{ThemeResource TextFillColorPrimaryBrush}"
                               VerticalAlignment="Center" />
                    
                    <!-- Action Buttons -->
                    <StackPanel Grid.Column="1" 
                                Orientation="Horizontal" 
                                Spacing="8">
                        
                        <!-- View Context Button -->
                        <AppBarButton x:Name="ViewContextButton"
                                      Click="ViewContextButton_Click"
                                      Width="32"
                                      Height="32"
                                      ToolTipService.ToolTip="View context JSON">
                            <AppBarButton.Icon>
                                <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}"
                                          Glyph="&#xE890;"/>
                            </AppBarButton.Icon>
                        </AppBarButton>
                        
                        <!-- Refresh Button -->
                        <AppBarButton x:Name="RefreshButton"
                                      Click="RefreshButton_Click"
                                      Width="32"
                                      Height="32"
                                      ToolTipService.ToolTip="Refresh context files">
                            <AppBarButton.Icon>
                                <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}"
                                          Glyph="&#xE72C;"/>
                            </AppBarButton.Icon>
                        </AppBarButton>
                        
                    </StackPanel>
                </Grid>
                
                <!-- Divider -->
                <Border Height="1" 
                        Background="{ThemeResource DividerStrokeColorDefaultBrush}" 
                        Margin="0,0,0,8" />
                
                <!-- Context Files List -->
                <StackPanel x:Name="ContextFilesContainer" Spacing="8">
                    <!-- Files will be loaded here dynamically -->
                </StackPanel>
                
                <!-- Empty State -->
                <TextBlock x:Name="EmptyStateText"
                           Text="No context"
                           FontSize="14"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                           HorizontalAlignment="Center"
                           Margin="0,32,0,0"
                           Visibility="Visible" />
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
